name: troll_public

channels:
  - default
  - pytorch
  - nvidia
  - conda-forge

dependencies:
  - python=3.10
  - cuda=12.4
  - cudnn=9.10
  - libxcrypt  # needed for triton
  - hydra-submitit-launcher==1.2.0
  - pip
  - pip:
    - hydra-list-sweeper==1.1.0